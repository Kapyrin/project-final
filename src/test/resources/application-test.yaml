#spring.cache.type: none
#spring:
#  init:
#    mode: always
#  datasource:
#    url: jdbc:postgresql://localhost:5433/jira-test
#    username: jira
#    password: JiraRush

#spring.cache.type: none
#spring:
#  test:
#    database:
#      replace: none
#  init:
#    mode: always
#  datasource:
#    url: jdbc:h2:mem:jira-test;NON_KEYWORDS=VALUE
#    username: sa
#    password:
#  liquibase:
#    change-log: h2-changelog.sql
#
spring.cache.type: none
spring:
  test:
    database:
      replace: none
  init:
    mode: always
  datasource:
    url: jdbc:h2:mem:jira-test;NON_KEYWORDS=VALUE
    username: sa
    password:
  liquibase:
    change-log: h2-changelog.sql
  prod:
    database:
      replace: none
      init:
        mode: always
      datasource:
        url: jdbc:postgresql://localhost:5433/jira-test
        username: jira
        password: JiraRush
    liquibase:
      change-log: db/changelog.sql"
